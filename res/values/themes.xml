<?xml version="1.0" encoding="utf-8"?>
<resources
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.

    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:colorPrimary" tools:ignore="NewApi">@color/blue_light</item>
        <item name="android:colorPrimaryDark" tools:ignore="NewApi">@color/blue_dark</item>
        <item name="android:statusBarColor" tools:ignore="NewApi">#4972af</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:windowContentTransitions" tools:ignore="NewApi">true</item>

        <!-- Support library compatibility -->
        <item name="colorPrimary">@color/blue_light</item>
        <item name="colorPrimaryDark">@color/blue_dark</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <!--<item name="android:windowAnimationStyle">@style/ActivityAnimation</item>-->
    </style>

    <style name="AppTheme.Default">
        <item name="actionbarColor">@color/blue_dark</item>
    </style>

    <style name="ImageViewerTheme" parent="Theme.AppCompat">
        <item name="android:colorPrimary" tools:ignore="NewApi">@android:color/transparent</item>
        <item name="android:colorPrimaryDark" tools:ignore="NewApi">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor" tools:ignore="NewApi">@android:color/transparent</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowTranslucentStatus" tools:ignore="NewApi">true</item>
        <item name="android:windowTranslucentNavigation" tools:ignore="NewApi">true</item>

        <!-- Support library compatibility -->
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="colorPrimaryDark">@android:color/transparent</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="actionbarColor">#33000000</item>
    </style>

</resources>